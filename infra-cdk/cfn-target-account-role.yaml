AWSTemplateFormatVersion: '2010-09-09'
Description: >
  AWSops Multi-Account Support - Target Account IAM Role.
  Deploy this stack in each target (non-host) AWS account to enable
  cross-account monitoring from the AWSops dashboard.

Parameters:
  HostAccountId:
    Type: String
    AllowedPattern: '^\d{12}$'
    ConstraintDescription: Must be a 12-digit AWS Account ID
    Description: AWS Account ID of the host account (where AWSops EC2 runs)

  RoleName:
    Type: String
    Default: 'AWSopsReadOnlyRole'
    AllowedPattern: '^[\w+=,.@-]+$'
    Description: IAM role name to create in this account

  ExternalId:
    Type: String
    Default: ''
    Description: External ID for STS AssumeRole confused deputy protection. Leave blank to skip.

Conditions:
  HasExternalId: !Not [!Equals [!Ref ExternalId, '']]

Resources:
  AWSopsReadOnlyRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Ref RoleName
      Description: Read-only role for AWSops dashboard cross-account monitoring
      MaxSessionDuration: 3600
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              AWS: !Sub 'arn:aws:iam::${HostAccountId}:root'
            Action: 'sts:AssumeRole'
            Condition: !If
              - HasExternalId
              - StringEquals:
                  'sts:ExternalId': !Ref ExternalId
              - !Ref 'AWS::NoValue'
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/ReadOnlyAccess
      Tags:
        - Key: Application
          Value: AWSops
        - Key: Purpose
          Value: CrossAccountMonitoring
        - Key: ManagedBy
          Value: CloudFormation

  # Additional permissions not included in ReadOnlyAccess
  AWSopsAdditionalPolicy:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: AWSopsAdditionalReadAccess
      Roles:
        - !Ref AWSopsReadOnlyRole
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          # Cost Explorer (not in ReadOnlyAccess)
          - Sid: CostExplorerAccess
            Effect: Allow
            Action:
              - 'ce:GetCostAndUsage'
              - 'ce:GetCostForecast'
              - 'ce:GetDimensionValues'
              - 'ce:GetTags'
              - 'ce:GetReservationUtilization'
              - 'ce:GetSavingsPlansUtilization'
            Resource: '*'
          # Budgets
          - Sid: BudgetsAccess
            Effect: Allow
            Action:
              - 'budgets:ViewBudget'
              - 'budgets:DescribeBudget*'
            Resource: '*'
          # Pricing (not in ReadOnlyAccess)
          - Sid: PricingAccess
            Effect: Allow
            Action:
              - 'pricing:GetProducts'
              - 'pricing:DescribeServices'
              - 'pricing:GetAttributeValues'
            Resource: '*'
          # CloudWatch extended (Log Insights query)
          - Sid: CloudWatchLogsInsights
            Effect: Allow
            Action:
              - 'logs:StartQuery'
              - 'logs:GetQueryResults'
              - 'logs:StopQuery'
            Resource: '*'
          # IAM policy simulation (for Security Gateway)
          - Sid: IAMPolicySimulation
            Effect: Allow
            Action:
              - 'iam:SimulatePrincipalPolicy'
              - 'iam:SimulateCustomPolicy'
            Resource: '*'
          # Network reachability analysis
          - Sid: ReachabilityAnalysis
            Effect: Allow
            Action:
              - 'ec2:CreateNetworkInsightsPath'
              - 'ec2:StartNetworkInsightsAnalysis'
              - 'ec2:DeleteNetworkInsightsPath'
              - 'ec2:DeleteNetworkInsightsAnalysis'
            Resource: '*'

Outputs:
  RoleArn:
    Description: ARN of the cross-account role for Steampipe and Lambda
    Value: !GetAtt AWSopsReadOnlyRole.Arn
    Export:
      Name: !Sub '${AWS::StackName}-RoleArn'

  RoleName:
    Description: Role name
    Value: !Ref AWSopsReadOnlyRole

  SteampipeProfile:
    Description: AWS CLI profile config to add on AWSops EC2 (~/.aws/config)
    Value: !Sub |
      [profile awsops-${AWS::AccountId}]
      role_arn = ${AWSopsReadOnlyRole.Arn}
      credential_source = Ec2InstanceMetadata
      region = ap-northeast-2

  TestCommand:
    Description: Test command to verify cross-account access from AWSops EC2
    Value: !Sub >-
      aws sts assume-role
      --role-arn ${AWSopsReadOnlyRole.Arn}
      --role-session-name awsops-test
      --query 'Credentials.AccessKeyId'
      --output text
